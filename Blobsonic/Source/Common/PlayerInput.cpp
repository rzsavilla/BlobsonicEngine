#include <stdafx.h>
#include "PlayerInput.h"
/*
void PlayerInput::handleKeyPress(KeyEvent* e)
{
	int ei= (unsigned int)e->iKey;
	switch ((unsigned int)e->iKey)
	{	
	case GLFW_KEY_W:
		m_ptrLocalMessages->push_back(std::make_shared<SetPositionMessage>(glm::vec3(100.0f, 100.0f, 100.0f)));
		break;
	case GLFW_KEY_S:

		break;
	default:
		break;
	}
}

PlayerInput::PlayerInput()
{
	m_sComponentID = "PlayerInput";
}

void PlayerInput::init()
{

}

void PlayerInput::handleMessage(Message * msg)
{
	if (msg->sID == "KeyEvent") {
		KeyEvent *e = (KeyEvent*)(msg);
		if (e->isDown) {
			handleKeyPress(e);
		}
	}
}

void PlayerInput::update(float dt)
{

}
*/